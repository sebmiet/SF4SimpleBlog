{% extends 'admin/admin.html.twig' %}
{#{% block header %}{{ title }}{% endblock %}#}
{% block body %}
    <article>
        <div class="container">
            <div class="row">
                <div class="col-lg-12 col-md-12 mx-auto">{#col-lg-8 col-md-10 mx-auto#}
                    <h4>Dodaj wpis:</h4>

                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    {{ form_end(form) }}



                </div>
            </div>
        </div>
    </article>

{% endblock %}