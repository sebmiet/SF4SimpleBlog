{% extends 'admin/admin.html.twig' %}
{#{% block header %}{{ title }}{% endblock %}#}
{% block body %}
    <article>
        <div class="container">
            <div class="row">
                <div class="col-lg-12 col-md-12 mx-auto">
                    <h4>Edytuj wpis:</h4>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </article>
{% endblock %}